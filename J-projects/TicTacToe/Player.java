public class Player implements Combination{
	int array[];
	array = new int[numPos]; //tracks position of each move first pos, second...

	public class RandomPlayer{
		select(random(position)) //until end of game
	}

	public class SmartPlayer implements Combination{     //learns using win/lose combo
		//use win combo only
		if(win == true){
			do again
		}else{
			RandomPlayer;
	}
}