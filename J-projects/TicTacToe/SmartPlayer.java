public class SmartPlayer implements Combination{     //learns using win/lose combo
			//use win combo only
	if(win == true){
		do(Combination);
		if(Combination != possible){ //if combination is taken or a square is already used
			RandomPlayer;
		}
	}else{
		RandomPlayer;
	}
}
public Class RandomPlayer{
	select(random(position)); //until end of game
}